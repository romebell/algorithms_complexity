function example2(array) {
    for (let i = 0; i <= array.length; i++) {
        for (let i = 0; i < array.length; i++) {
            // do work
        }
    }

    for(let k = 0; k < array.length; k++) {
        // do more work
    }
}

// Time Complexity ? 














// T(n^2 + n) =>  O(n^2)